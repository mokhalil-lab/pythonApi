[project]
name = "pythonapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mokhalil", email = "mo7ammedkhalil@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "psycopg[binary]>=3.3.5",
]


[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
